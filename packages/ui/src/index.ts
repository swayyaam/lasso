// Shared Lasso UI components and design tokens.
//
// Phase E turns docs/design.md into tokens.ts (the single source of colour,
// type, spacing and radius values), a Tailwind preset, and the primitive
// components. Nothing here hardcodes a hex value.
export {};

// Command lasso is the Wails desktop shell for Lasso.
//
// Phase A scaffold: the Wails application, bindings, and event wiring land in
// phase D. This stub exists so the module builds and the workspace resolves.
package main

import "fmt"

func main() {
	fmt.Println("lasso: desktop shell not wired up yet (phase D)")
}
